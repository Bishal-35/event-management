<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Management System</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <div class="container">
        <h2 style="text-align: center;">Ecstasy Event Management System</h2>
        <h3>Event Details</h3>
        
        <table border="1">
            <thead>
                <tr>
                    <th>Event ID</th>
                    <th>Event Name</th>
                    <th>Details</th>
                    <th>Edit</th>
                    <th>Delete Event</th>
                </tr>
            </thead>
            <tbody>
                <% events.forEach(event => { %>
                    <tr>
                        <td><%= event.id %></td>
                        <td><%= event.name %></td>
                        <td><a href="./show/<%= event.id %>">Show details</a></td>
                        <td><a href="./edit/<%= event.id %>">Edit</a></td>
                        <td><a href="./delete/<%= event.id %>">Delete</a></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
        <a href="/add">Add Event</a>
    </div>
</body>
</html>